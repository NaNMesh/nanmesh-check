{
  "name": "nanmesh-check",
  "version": "0.1.0",
  "description": "GitHub Action that gates AI agent deploys on the NaN Mesh shared operational memory. Fails CI if any tool in your manifest has critical unresolved failures or low confidence for your stack.",
  "main": "dist/index.js",
  "type": "module",
  "scripts": {
    "build": "tsc && ncc build src/index.ts -o dist --source-map",
    "test": "echo 'placeholder — Phase 6.1'"
  },
  "author": "NaN Logic LLC",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/NaNMesh/nanmesh-check.git"
  },
  "keywords": [
    "github-action",
    "ai-agents",
    "trust",
    "nanmesh",
    "mcp",
    "langchain",
    "crewai",
    "ci-gate"
  ],
  "dependencies": {
    "@actions/core": "^1.10.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "@vercel/ncc": "^0.38.0",
    "typescript": "^5.5.0"
  },
  "engines": {
    "node": ">=20"
  }
}
